# ============================================================
# Statistical Audit Platform — Dependencies
# Python >= 3.10
#
# NOTE IMPORTANTE SUR LA COMPATIBILITÉ :
# factor-analyzer utilise sklearn.utils.validation.check_array()
# avec le paramètre `force_all_finite` renommé `ensure_all_finite`
# dans scikit-learn >= 1.6. On épingle scikit-learn < 1.6.
# ============================================================

# --- Core Data ---
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# --- Database ---
sqlalchemy>=2.0.0,<3.0.0

# --- Machine Learning & Statistics ---
# IMPORTANT : scikit-learn doit rester < 1.6 (compatibilité factor_analyzer)
scikit-learn>=1.3.0,<1.6.0
scipy>=1.11.0,<2.0.0
factor-analyzer>=0.4.1,<1.0.0
statsmodels>=0.14.0,<1.0.0
pingouin>=0.5.4,<1.0.0

# --- Visualisation ---
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
plotly>=5.15.0,<6.0.0

# --- UI ---
streamlit>=1.30.0,<2.0.0

# --- LLM ---
anthropic>=0.25.0,<1.0.0

# --- Dev / Test ---
pytest>=7.4.0
pytest-cov>=4.1.0